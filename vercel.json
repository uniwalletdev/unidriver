{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'Wrong Vercel root. API runs on Railway (root Dockerfile). Web app needs Root Directory=apps/web. See docs/DEPLOYMENT.md.' && exit 1"
}
